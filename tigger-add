#! /bin/dash

# add files to index
# only ordinary file can be added
# tigger-add: error: can not open 'b'


# for fname in "$@"
# do 
#     # count=0
#     # backup=$fname.$count
#     # while [ "$(find .tigger/$fname 2>/dev/null)" = "$backup" ] 
#     # do
#     #     count=$(($count+1))
#     #     bname=$fname.$count
#     # done
#     # cat $fname > $backup


#     # get current branch
#     branch="$(cat .tigger/HEAD)"
#     cat $fname > .tigger/branches/$branch/index/$fname
# done

branch="$(cat .tigger/HEAD)"
cp "$@" .tigger/branches/"$branch"/index
